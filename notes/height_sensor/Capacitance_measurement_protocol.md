## Capacitance measurement protocol

### Preliminary requirements:
- The capacitive gauge wires are mounted stretched in a robust manner.
- How to read a data file.
- How is the data saved in real time?
- What is the external offset? Is it stable under mild perturbations? (introduce manually)
- Can I measure external offset during a run?

### Linearization


### Run
- External capacitive offset: measure once, before experiment, at varying temperatures if possible. Make sure that the physical configuration doesn't change significantly during the experiment.

